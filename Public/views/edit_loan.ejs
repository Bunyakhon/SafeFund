<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>SafeFund - แก้ไขสัญญาเงินกู้</title>
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@200..800&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="../css/style.css"> 
</head>
<body>
    <div class="sidebar-overlay" id="sidebarOverlay"></div>
    <aside>
        <div class="branding">
            <div class="logo-icon"><span class="material-symbols-outlined">shield_person</span></div>
            <div class="brand-text">
                <h1>SafeFund</h1>
                <p>Village Mgmt</p>
            </div>
        </div>
        <nav>
            <a class="nav-item" href="#"><span class="material-symbols-outlined">dashboard</span> Dashboard</a>
            <a class="nav-item" href="/"><span class="material-symbols-outlined">group</span> จัดการสมาชิก</a>
            <a class="nav-item active" href="/loans"><span class="material-symbols-outlined">account_balance_wallet</span> จัดการเงินกู้</a>
            <a class="nav-item" href="#"><span class="material-symbols-outlined">savings</span> เงินฝากสัจจะ</a>
            <a class="nav-item" href="#"><span class="material-symbols-outlined">assessment</span> รายงาน</a>
        </nav>
        <div class="user-profile-card">
            <div class="user-box">
                <p>Logged in as Admin</p>
                <div class="user-info">
                    <div class="avatar"></div>
                    <div class="user-name">วิชญ์พล สมใจ</div>
                </div>
            </div>
        </div>
    </aside>

    <div class="main-wrapper">
        <header>
            <div class="header-left" style="display: flex; align-items: center;">
                <button class="mobile-menu-btn" id="mobileMenuBtn"><span class="material-symbols-outlined">menu</span></button>
                <h2 style="font-weight: 700; font-size: 1.125rem;">ระบบจัดการกองทุนหมู่บ้าน</h2>
            </div>
            <div class="header-actions">
                <button class="icon-btn" id="darkModeBtn" title="สลับโหมดมืด/สว่าง">
                    <span class="material-symbols-outlined" id="darkModeIcon">dark_mode</span>
                </button>
            </div>
        </header>

        <main>
            <div class="page-header">
                <div class="page-title">
                    <h1>แก้ไขสัญญาเงินกู้</h1>
                    <p>อัปเดตรายละเอียดหรือเปลี่ยนสถานะสัญญาเงินกู้หมายเลข LN-<%= loan.loan_id %></p>
                </div>
            </div>

            <div class="content-card form-card">
                <div class="form-header-icon">
                    <div class="icon-circle"><span class="material-symbols-outlined">edit_note</span></div>
                    <h2 style="font-size: 1.25rem; font-weight: 700; color: var(--text-main); margin: 0;">รายละเอียดสัญญา</h2>
                </div>

                <form action="/update-loan/<%= loan.loan_id %>" method="POST">
                    <div class="form-row">
                        <div class="form-group">
                            <label>รหัสสัญญา (Loan ID)</label>
                            <input type="text" value="LN-<%= loan.loan_id %>" readonly class="input-readonly">
                        </div>
                        <div class="form-group">
                            <label>วันที่ทำสัญญา</label>
                            <input type="text" value="<%= new Date(loan.createdAt).toLocaleDateString('th-TH') %>" readonly class="input-readonly">
                        </div>
                    </div>

                    <div class="form-group">
                        <label>สมาชิกผู้กู้ <span class="text-danger">*</span></label>
                        <select name="member_id" required style="width: 100%; padding: 12px; border-radius: 8px; border: 1px solid var(--border-color); background: var(--card-bg); color: var(--text-main);">
                            <% members.forEach(function(member) { %>
                                <option value="<%= member.member_id %>" <%= member.member_id === loan.member_id ? 'selected' : '' %>>
                                    ID: <%= member.member_id %> | <%= member.member_name %>
                                </option>
                            <% }); %>
                        </select>
                    </div>

                    <div class="form-row">
                        <div class="form-group">
                            <label>ยอดเงินกู้ (บาท) <span class="text-danger">*</span></label>
                            <input type="number" name="loan_amount" step="0.01" value="<%= loan.loan_amount %>" required>
                        </div>
                        <div class="form-group">
                            <label>อัตราดอกเบี้ย (%) <span class="text-danger">*</span></label>
                            <input type="number" name="interest_rate" step="0.1" value="<%= loan.interest_rate %>" required>
                        </div>
                    </div>

                    <div class="form-row">
                        <div class="form-group">
                            <label>ระยะเวลาผ่อนชำระ (เดือน) <span class="text-danger">*</span></label>
                            <input type="number" name="duration_months" value="<%= loan.duration_months %>" min="1" required>
                        </div>
                        <div class="form-group">
                            <label>สถานะสัญญา</label>
                            <select name="status" style="width: 100%; padding: 12px; border-radius: 8px; border: 1px solid var(--border-color); background: var(--card-bg); color: var(--text-main);">
                                <option value="รอพิจารณา" <%= loan.status === 'รอพิจารณา' ? 'selected' : '' %>>รอพิจารณา</option>
                                <option value="อนุมัติแล้ว" <%= loan.status === 'อนุมัติแล้ว' ? 'selected' : '' %>>อนุมัติแล้ว</option>
                                <option value="ปิดยอดแล้ว" <%= loan.status === 'ปิดยอดแล้ว' ? 'selected' : '' %>>ปิดยอดแล้ว</option>
                            </select>
                        </div>
                    </div>

                    <div class="form-divider"></div>

                    <div class="form-actions">
                        <button type="button" class="btn-cancel" onclick="window.location.href='/loans'">
                            ยกเลิก
                        </button>
                        <button type="submit" class="btn-submit">
                            <span class="material-symbols-outlined">save</span> บันทึกการแก้ไข
                        </button>
                    </div>
                </form>
            </div>
        </main>
    </div>

    <script src="../js/member-management.js"></script>
</body>
</html>