<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>SafeFund - รายงานสรุป</title>
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@200..800&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="sidebar-overlay" id="sidebarOverlay"></div>
    <aside>
        <div class="branding">
            <div class="logo-icon"><span class="material-symbols-outlined">shield_person</span></div>
            <div class="brand-text">
                <h1>SafeFund</h1>
                <p>Village Mgmt</p>
            </div>
        </div>
        <nav>
            <a class="nav-item" href="/"><span class="material-symbols-outlined">group</span> จัดการสมาชิก</a>
            <a class="nav-item" href="/loans"><span class="material-symbols-outlined">account_balance_wallet</span> จัดการเงินกู้</a>
            <a class="nav-item" href="/savings"><span class="material-symbols-outlined">savings</span> เงินฝากสัจจะ</a>
            <a class="nav-item active" href="/reports"><span class="material-symbols-outlined">assessment</span> รายงาน</a>
        </nav>
        <div class="user-profile-card">
            <div class="user-box">
                <p>Logged in as Admin</p>
                <div class="user-info">
                    <div class="avatar"></div>
                    <div class="user-name">Admin Administered</div>
                </div>
            </div>
        </div>
    </aside>

    <div class="main-wrapper">
        <header>
            <div class="header-left">
                <button class="mobile-menu-btn" id="mobileMenuBtn"><span class="material-symbols-outlined">menu</span></button>
                <h2>ระบบจัดการกองทุนหมู่บ้าน</h2>
            </div>
            <div class="header-actions">
                <button class="icon-btn" id="darkModeBtn" title="สลับโหมดมืด/สว่าง">
                    <span class="material-symbols-outlined" id="darkModeIcon">dark_mode</span>
                </button>
            </div>
        </header>

        <main>
            <div class="page-header">
                <div class="page-title">
                    <h1>รายงานสรุปผลดำเนินงาน</h1>
                    <p>สรุปสถิติภาพรวมและการดำเนินงานทั้งหมดของกองทุน SafeFund</p>
                </div>
            </div>

            <%
                // --- ส่วนการคำนวณข้อมูลสถิติรวม ---
                let outstandingReport = {};
                let totalLoanPrincipalSum = 0;   
                let totalFundPaid = 0;        
                let totalFundRemaining = 0;   

                loans.forEach(loan => {
                    totalLoanPrincipalSum += parseFloat(loan.loan_amount); 
                    
                    let mid = loan.member_id;
                    if (loan.Member) {
                        if (!outstandingReport[mid]) {
                            outstandingReport[mid] = { 
                                name: loan.Member.member_name, 
                                principal: 0,
                                balance: 0, 
                                paid: 0, 
                                remainingPeriods: 0 
                            };
                        }
                        outstandingReport[mid].principal += parseFloat(loan.loan_amount);
                        if (loan.payments) {
                            loan.payments.forEach(p => {
                                let amt = parseFloat(p.amount);
                                if (p.status !== 'Paid') {
                                    outstandingReport[mid].balance += amt;
                                    outstandingReport[mid].remainingPeriods++;
                                    totalFundRemaining += amt; 
                                } else {
                                    outstandingReport[mid].paid += amt;
                                    totalFundPaid += amt; 
                                }
                            });
                        }
                    }
                });

                let savingsReport = {};
                let totalFundSavings = 0; 
                savings.forEach(s => {
                    if (s.Member) {
                        let mid = s.member_id;
                        if (!savingsReport[mid]) {
                            savingsReport[mid] = { name: s.Member.member_name, total: 0, count: 0 };
                        }
                        let amt = parseFloat(s.deposit_amount);
                        savingsReport[mid].total += amt;
                        savingsReport[mid].count++;
                        totalFundSavings += amt;
                    }
                });
            %>

            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-icon" style="background-color: rgba(10, 102, 194, 0.1); color: #0a66c2;">
                        <span class="material-symbols-outlined">account_balance</span>
                    </div>
                    <div class="stat-info">
                        <p>ยอดเงินกู้ทั้งหมด (เงินต้น)</p>
                        <p style="font-size: 1.5rem; font-weight: 800;">
                            ฿ <%= totalLoanPrincipalSum.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                        </p>
                    </div>
                </div>

                <div class="stat-card">
                    <div class="stat-icon" style="background-color: rgba(40, 167, 69, 0.1); color: #28a745;">
                        <span class="material-symbols-outlined">payments</span>
                    </div>
                    <div class="stat-info">
                        <p>ยอดชำระคืนมาแล้วรวม</p>
                        <p style="font-size: 1.5rem; font-weight: 800; color: #28a745;">
                            ฿ <%= totalFundPaid.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                        </p>
                    </div>
                </div>

                <div class="stat-card">
                    <div class="stat-icon" style="background-color: rgba(220, 53, 69, 0.1); color: #dc3545;">
                        <span class="material-symbols-outlined">pending_actions</span>
                    </div>
                    <div class="stat-info">
                        <p>ยอดหนี้คงเหลือทั้งหมด</p>
                        <p style="font-size: 1.5rem; font-weight: 800; color: #dc3545;">
                            ฿ <%= totalFundRemaining.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                        </p>
                    </div>
                </div>
            </div>

            <h2 class="report-section-title">
                <span class="material-symbols-outlined">account_balance_wallet</span> 
                รายละเอียดหนี้คงเหลือรายบุคคล
            </h2>

            <div class="content-card" style="margin-bottom: 32px;">
                <div class="table-container">
                    <table>
                        <thead>
                            <tr>
                                <th class="sortable" data-type="text">ชื่อ-นามสกุล <span class="material-symbols-outlined sort-icon">expand_more</span></th>
                                <th class="sortable" data-type="number" style="text-align: right;">ยอดกู้ (บาท) <span class="material-symbols-outlined sort-icon">expand_more</span></th>
                                <th class="sortable" data-type="number" style="text-align: right;">ชำระแล้ว (บาท) <span class="material-symbols-outlined sort-icon">expand_more</span></th>
                                <th class="sortable" data-type="number" style="text-align: right;">หนี้คงเหลือ (บาท) <span class="material-symbols-outlined sort-icon">expand_more</span></th>
                                <th class="sortable" data-type="number" style="text-align: center;">งวดที่เหลือ <span class="material-symbols-outlined sort-icon">expand_more</span></th>
                            </tr>
                        </thead>
                        <tbody>
                            <% for (let id in outstandingReport) { if(outstandingReport[id].balance > 0 || outstandingReport[id].paid > 0) { %>
                                <tr>
                                    <td style="font-weight: 600;"><%= outstandingReport[id].name %></td>
                                    <td style="text-align: right;" data-value="<%= outstandingReport[id].principal %>">
                                        ฿ <%= outstandingReport[id].principal.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                                    </td>
                                    <td style="text-align: right; color: #28a745;" data-value="<%= outstandingReport[id].paid %>">
                                        ฿ <%= outstandingReport[id].paid.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                                    </td>
                                    <td style="text-align: right; color: #dc3545; font-weight: 700;" data-value="<%= outstandingReport[id].balance %>">
                                        ฿ <%= outstandingReport[id].balance.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                                    </td>
                                    <td style="text-align: center;" data-value="<%= outstandingReport[id].remainingPeriods %>">
                                        <span class="badge" style="background: rgba(108, 117, 125, 0.1); color: #6c757d; padding: 4px 12px; border-radius: 20px;">
                                            <%= outstandingReport[id].remainingPeriods %> งวด
                                        </span>
                                    </td>
                                </tr>
                            <% }} %>
                        </tbody>
                    </table>
                </div>
            </div>

            <h2 class="report-section-title">
                <span class="material-symbols-outlined">savings</span> 
                รายละเอียดเงินฝากสัจจะและปันผล
            </h2>

            <div class="stats-grid">
                <div class="stat-card" style="grid-column: span 2;">
                    <div class="stat-icon" style="background: rgba(10, 102, 194, 0.1); color: #0a66c2;">
                        <span class="material-symbols-outlined">account_balance_wallet</span>
                    </div>
                    <div class="stat-info" style="display: flex; justify-content: space-between; width: 100%; align-items: center;">
                        <div>
                            <p>ยอดเงินฝากสัจจะรวมทั้งหมด</p>
                            <p style="font-size: 1.5rem; font-weight: 800; color: #0a66c2;">
                                ฿ <%= totalFundSavings.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                            </p>
                        </div>
                        <div style="text-align: right; border-left: 2px solid var(--border-color); padding-left: 20px;">
                            <p>หนี้คงเหลือรวม</p>
                            <p style="font-size: 1.5rem; font-weight: 800; color: #dc3545;">
                                ฿ <%= totalFundRemaining.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                            </p>
                        </div>
                    </div>
                </div>
            </div>

            <div class="content-card">
                <div class="table-container">
                    <table id="savingReportTable">
                        <thead>
                            <tr>
                                <th class="sortable" data-type="text">ชื่อ-นามสกุล <span class="material-symbols-outlined sort-icon">expand_more</span></th>
                                <th class="sortable" data-type="number">จำนวนครั้ง <span class="material-symbols-outlined sort-icon">expand_more</span></th>
                                <th class="sortable" data-type="number" style="text-align: right;">ยอดเงินสะสม (บาท) <span class="material-symbols-outlined sort-icon">expand_more</span></th>
                                <th class="sortable" data-type="number" style="text-align: right;">ปันผล (3%) <span class="material-symbols-outlined sort-icon">expand_more</span></th>
                                <th class="sortable" data-type="number" style="text-align: right; font-weight: 700;">รวมเงินปันผล (บาท) <span class="material-symbols-outlined sort-icon">expand_more</span></th>
                            </tr>
                        </thead>
                        <tbody>
                            <% for (let id in savingsReport) { 
                                let totalDeposit = savingsReport[id].total;
                                let dividend = totalDeposit * 0.03;
                                let grandTotal = totalDeposit + dividend;
                            %>
                                <tr>
                                    <td style="font-weight: 600;"><%= savingsReport[id].name %></td>
                                    <td data-value="<%= savingsReport[id].count %>"><%= savingsReport[id].count %> ครั้ง</td>
                                    <td style="text-align: right;" data-value="<%= totalDeposit %>">
                                        ฿ <%= totalDeposit.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                                    </td>
                                    <td style="text-align: right; color: #28a745;" data-value="<%= dividend %>">
                                        ฿ <%= dividend.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                                    </td>
                                    <td style="text-align: right; color: #0a66c2; font-weight: 700;" data-value="<%= grandTotal %>">
                                        ฿ <%= grandTotal.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                                    </td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </main>
    </div>

    <script src="/js/member-management.js"></script>
</body>
</html>