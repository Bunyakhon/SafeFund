<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>SafeFund - รายงานสรุป</title>
    <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@200..800&display=swap" rel="stylesheet" />
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="/css/style.css">
    <style>
        /* สไตล์เพิ่มเติมสำหรับ Dropdown และการซ่อนส่วนประกอบ */
        .report-header-group {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 16px;
            flex-wrap: wrap;
            gap: 10px;
        }

        .view-selector {
            padding: 8px 12px;
            border-radius: 8px;
            /* แก้ไขตรงนี้เพื่อให้เป็นสีขาวคงที่ */
            background-color: #ffffff !important; 
            background: #ffffff !important;
            color: #333333 !important; /* กำหนดสีตัวอักษรเป็นสีเข้มเพื่อให้ชัดเจนบนพื้นขาว */
            border: 1px solid var(--border-color);
            font-family: 'Manrope', sans-serif;
            font-size: 0.9rem;
            cursor: pointer;
            outline: none;
            transition: all 0.2s ease;
        }

        .view-selector:hover {
            border-color: #0a66c2;
        }

        .hidden-section {
            display: none !important;
        }
    </style>
</head>

<body>
    <div class="sidebar-overlay" id="sidebarOverlay"></div>
    <aside>
        <div class="branding">
            <div class="logo-icon"><span class="material-symbols-outlined">shield_person</span></div>
            <div class="brand-text">
                <h1>SafeFund</h1>
                <p>Village Mgmt</p>
            </div>
        </div>
        <nav>
            <a class="nav-item" href="/"><span class="material-symbols-outlined">group</span> จัดการสมาชิก</a>
            <a class="nav-item" href="/loans"><span class="material-symbols-outlined">account_balance_wallet</span> จัดการเงินกู้</a>
            <a class="nav-item" href="/savings"><span class="material-symbols-outlined">savings</span> เงินฝากสัจจะ</a>
            <a class="nav-item active" href="/reports"><span class="material-symbols-outlined">assessment</span> รายงาน</a>
        </nav>
        <div class="user-profile-card">
            <div class="user-box">
                <p>Logged in as Admin</p>
                <div class="user-info">
                    <div class="avatar"></div>
                    <div class="user-name">Admin Administered</div>
                </div>
            </div>
        </div>
    </aside>

    <div class="main-wrapper">
        <header>
            <div class="header-left">
                <button class="mobile-menu-btn" id="mobileMenuBtn"><span class="material-symbols-outlined">menu</span></button>
                <h2>ระบบจัดการกองทุนหมู่บ้าน</h2>
            </div>
            <div class="header-actions">
                <button class="icon-btn" id="darkModeBtn" title="สลับโหมดมืด/สว่าง">
                    <span class="material-symbols-outlined" id="darkModeIcon">dark_mode</span>
                </button>
            </div>
        </header>

        <main>
            <div class="page-header">
                <div class="page-title">
                    <h1>รายงานสรุปผลดำเนินงาน</h1>
                    <p>สรุปสถิติภาพรวมและการดำเนินงานทั้งหมดของกองทุน SafeFund</p>
                </div>
            </div>

            <%
                /* --- ส่วนการคำนวณข้อมูลสถิติรวม --- */
                let outstandingReport = {};
                let totalLoanPrincipalSum = 0;   
                let totalFundPaid = 0;        
                let totalFundRemaining = 0;

                loans.forEach(loan => {
                    totalLoanPrincipalSum += parseFloat(loan.loan_amount); 
                    
                    let mid = loan.member_id;
                    if (loan.Member) {
                        if (!outstandingReport[mid]) {
                            outstandingReport[mid] = { 
                                name: loan.Member.member_name, 
                                principal: 0,
                                balance: 0, 
                                paid: 0, 
                                remainingPeriods: 0 
                            };
                        }
                        outstandingReport[mid].principal += parseFloat(loan.loan_amount);
                        
                        if (loan.payments) {
                            loan.payments.forEach(p => {
                                let amt = parseFloat(p.amount);
                                if (p.status !== 'Paid') {
                                    outstandingReport[mid].balance += amt;
                                    outstandingReport[mid].remainingPeriods++;
                                    totalFundRemaining += amt; 
                                } else {
                                    outstandingReport[mid].paid += amt;
                                    totalFundPaid += amt; 
                                }
                            });
                        }
                    }
                });

                let savingsReport = {};
                let totalFundSavings = 0; 
                savings.forEach(s => {
                    if (s.Member) {
                        let mid = s.member_id;
                        if (!savingsReport[mid]) {
                            savingsReport[mid] = { name: s.Member.member_name, total: 0, count: 0 };
                        }
                        let amt = parseFloat(s.deposit_amount);
                        savingsReport[mid].total += amt;
                        savingsReport[mid].count++;
                        totalFundSavings += amt;
                    }
                });

                let treasuryBalance = totalFundSavings - totalFundRemaining;
                let totalFundDividend = totalFundSavings * 0.03; 
                let totalSavingsWithDividend = totalFundSavings + totalFundDividend;
                let totalLoanContractSum = totalFundPaid + totalFundRemaining;
            %>

            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-icon" style="background-color: rgba(10, 102, 194, 0.1); color: #0a66c2;">
                        <span class="material-symbols-outlined">account_balance</span>
                    </div>
                    <div class="stat-info">
                        <p>ยอดเงินกู้ทั้งหมด (เงินต้น)</p>
                        <p style="font-size: 1.5rem; font-weight: 800;">
                            ฿ <%= totalLoanPrincipalSum.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                        </p>
                    </div>
                </div>

                <div class="stat-card">
                    <div class="stat-icon" style="background-color: rgba(40, 167, 69, 0.1); color: #28a745;">
                        <span class="material-symbols-outlined">payments</span>
                    </div>
                    <div class="stat-info">
                        <p>ยอดชำระคืนมาแล้วรวม</p>
                        <p style="font-size: 1.5rem; font-weight: 800; color: #28a745;">
                            ฿ <%= totalFundPaid.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                        </p>
                    </div>
                </div>

                <div class="stat-card">
                    <div class="stat-icon" style="background-color: rgba(220, 53, 69, 0.1); color: #dc3545;">
                        <span class="material-symbols-outlined">pending_actions</span>
                    </div>
                    <div class="stat-info">
                        <p>ยอดหนี้คงเหลือทั้งหมด</p>
                        <p style="font-size: 1.5rem; font-weight: 800; color: #dc3545;">
                            ฿ <%= totalFundRemaining.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                        </p>
                    </div>
                </div>

                <div class="stat-card" style="border: 1px solid #28a745;">
                    <div class="stat-icon" style="background-color: rgba(40, 167, 69, 0.1); color: #28a745;">
                        <span class="material-symbols-outlined">account_balance_wallet</span>
                    </div>
                    <div class="stat-info">
                        <p>ยอดเงินคงเหลือในคลัง</p>
                        <p style="font-size: 1.5rem; font-weight: 800; color: #28a745;">
                            ฿ <%= treasuryBalance.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                        </p>
                    </div>
                </div>
            </div>

            <div class="report-header-group">
                <h2 class="report-section-title" style="margin-bottom: 0;">
                    <span class="material-symbols-outlined">account_balance_wallet</span> 
                    รายละเอียดหนี้คงเหลือรายบุคคล
                </h2>
                <select class="view-selector" id="loanViewSelector">
                    <option value="summary">ยอดเงินกู้ทั้งหมด (เงินต้น)</option>
                    <option value="details">แสดงรายละเอียดของตาราง</option>
                </select>
            </div>

            <div class="stats-grid" id="loanSummary">
                <div class="stat-card" style="grid-column: span 2;">
                    <div class="stat-icon" style="background: rgba(10, 102, 194, 0.1); color: #0a66c2;">
                        <span class="material-symbols-outlined">summarize</span>
                    </div>
                    <div class="stat-info" style="display: flex; justify-content: space-between; width: 100%; align-items: center;">
                        <div style="display: flex; gap: 40px;">
                            <div>
                                <p>ยอดเงินกู้ทั้งหมด (เงินต้น)</p>
                                <p style="font-size: 1.5rem; font-weight: 800; color: var(--text-main);">
                                    ฿ <%= totalLoanPrincipalSum.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                                </p>
                            </div>
                            <div style="border-left: 2px solid var(--border-color); padding-left: 20px;">
                                <p>ยอดเงินกู้ทั้งหมดรวมดอกเบี้ย</p>
                                <p style="font-size: 1.5rem; font-weight: 800; color: #0a66c2;">
                                    ฿ <%= totalLoanContractSum.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                                </p>
                            </div>
                            <div style="text-align: right; border-left: 2px solid var(--border-color); padding-left: 20px;">
                                <p>หนี้คงเหลือรวม</p>
                                <p style="font-size: 1.5rem; font-weight: 800; color: #dc3545;">
                                    ฿ <%= totalFundRemaining.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="content-card hidden-section" id="loanTableContainer" style="margin-bottom: 32px;">
                <div class="table-container">
                    <table>
                        <thead>
                            <tr>
                                <th class="sortable" data-type="text">ชื่อ-นามสกุล</th>
                                <th style="text-align: right;">ยอดกู้ (บาท)</th>
                                <th style="text-align: right;">ชำระแล้ว (บาท)</th>
                                <th style="text-align: right;">หนี้คงเหลือ (บาท)</th>
                                <th style="text-align: center;">งวดที่เหลือ</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% for (let id in outstandingReport) { if(outstandingReport[id].balance > 0 || outstandingReport[id].paid > 0) { %>
                                <tr>
                                    <td style="font-weight: 600;">
                                        <a href="/loans" style="text-decoration: none; color: #0a66c2;">
                                            <%= outstandingReport[id].name %>
                                        </a>
                                    </td>
                                    <td style="text-align: right;">฿ <%= outstandingReport[id].principal.toLocaleString(undefined, {minimumFractionDigits: 2}) %></td>
                                    <td style="text-align: right; color: #28a745;">฿ <%= outstandingReport[id].paid.toLocaleString(undefined, {minimumFractionDigits: 2}) %></td>
                                    <td style="text-align: right; color: #dc3545; font-weight: 700;">฿ <%= outstandingReport[id].balance.toLocaleString(undefined, {minimumFractionDigits: 2}) %></td>
                                    <td style="text-align: center;"><%= outstandingReport[id].remainingPeriods %> งวด</td>
                                </tr>
                            <% }} %>
                        </tbody>
                    </table>
                </div>
            </div>

            <div class="report-header-group">
                <h2 class="report-section-title" style="margin-bottom: 0;">
                    <span class="material-symbols-outlined">savings</span> 
                    รายละเอียดเงินฝากสัจจะและปันผล
                </h2>
                <select class="view-selector" id="savingViewSelector">
                    <option value="summary">ยอดเงินฝากทั้งหมด</option>
                    <option value="details">แสดงรายละเอียดของตาราง</option>
                </select>
            </div>

            <div class="stats-grid" id="savingSummary">
                <div class="stat-card" style="grid-column: span 2;">
                    <div class="stat-icon" style="background: rgba(10, 102, 194, 0.1); color: #0a66c2;">
                        <span class="material-symbols-outlined">payments</span>
                    </div>
                    <div class="stat-info" style="display: flex; justify-content: space-between; width: 100%; align-items: center;">
                        <div style="display: flex; gap: 40px;">
                            <div>
                                <p>ยอดเงินฝากสะสมปัจจุบัน</p>
                                <p style="font-size: 1.5rem; font-weight: 800; color: var(--text-main);">
                                    ฿ <%= totalFundSavings.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                                </p>
                            </div>
                            <div style="border-left: 2px solid var(--border-color); padding-left: 20px;">
                                <p>ยอดเงินฝากรวมปันผล (3%)</p>
                                <p style="font-size: 1.5rem; font-weight: 800; color: #0a66c2;">
                                    ฿ <%= totalSavingsWithDividend.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                                </p>
                            </div>
                        </div>
                        <div style="text-align: right; border-left: 2px solid var(--border-color); padding-left: 20px;">
                            <p>ภาระปันผลที่ต้องจ่าย (3%)</p>
                            <p style="font-size: 1.5rem; font-weight: 800; color: #28a745;">
                                ฿ <%= totalFundDividend.toLocaleString(undefined, {minimumFractionDigits: 2}) %>
                            </p>
                        </div>
                    </div>
                </div>
            </div>

            <div class="content-card hidden-section" id="savingTableContainer">
                <div class="table-container">
                    <table>
                        <thead>
                            <tr>
                                <th>ชื่อ-นามสกุล</th>
                                <th>จำนวนครั้ง</th>
                                <th style="text-align: right;">ยอดเงินสะสม (บาท)</th>
                                <th style="text-align: right;">ปันผล (3%)</th>
                                <th style="text-align: right; font-weight: 700;">รวมเงินปันผล (บาท)</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% for (let id in savingsReport) { 
                                let totalDeposit = savingsReport[id].total;
                                let dividend = totalDeposit * 0.03;
                                let grandTotal = totalDeposit + dividend;
                            %>
                                <tr>
                                    <td style="font-weight: 600;">
                                        <a href="/savings/history/<%= id %>" style="text-decoration: none; color: #0a66c2;">
                                            <%= savingsReport[id].name %>
                                        </a>
                                    </td>
                                    <td><%= savingsReport[id].count %> ครั้ง</td>
                                    <td style="text-align: right;">฿ <%= totalDeposit.toLocaleString(undefined, {minimumFractionDigits: 2}) %></td>
                                    <td style="text-align: right; color: #28a745;">฿ <%= dividend.toLocaleString(undefined, {minimumFractionDigits: 2}) %></td>
                                    <td style="text-align: right; color: #0a66c2; font-weight: 700;">฿ <%= grandTotal.toLocaleString(undefined, {minimumFractionDigits: 2}) %></td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </main>
    </div>

    <script src="/js/member-management.js"></script>
</body>
</html>